# Omnipotent-Source-IP
把Proxy Protocol v2/v1 协议转换成功X-Real-IP或X-Forwarded-For 让原本不支持Proxy Protocol协议的软件获取到源IP
Convert the Proxy Protocol v2/v1 protocol to X-Real-IP or X-Forwarded-For so that software that originally does not support the Proxy Protocol can obtain the source IP.
